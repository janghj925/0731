# .github/workflows/hello.yml

name: Hello GitHub Actions

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v3  # <-- 이 부분 추가

      - name: print
        run: echo "hello git"
